name: EAS OTA Update

on:
  push:
    branches:
      - master
      - production

jobs:
  update:
    name: Publish OTA Update
    runs-on: ubuntu-latest
    
    steps:
      - name: Check out repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - name: Setup Expo and EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        run: npm ci

      - name: Determine branch and publish update
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/production" ]]; then
            echo "Publishing to production branch..."
            eas update --branch production --message "${{ github.event.head_commit.message }}" --non-interactive
          else
            echo "Publishing to preview branch..."
            eas update --branch preview --message "${{ github.event.head_commit.message }}" --non-interactive
          fi
